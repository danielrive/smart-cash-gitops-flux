apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-bank
  labels:
    app: bank
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bank
  template:
    metadata:
      labels:
        app: bank
    spec:
      serviceAccountName: sa-bank-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
      - name: bank-container
        image: service-image
        imagePullPolicy: Always
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
        ports:
        - containerPort: 8282
        livenessProbe:
          httpGet:
            port: 8282  
            path: /health  
          initialDelaySeconds: 1
          periodSeconds: 10
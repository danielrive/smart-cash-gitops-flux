apiVersion: v1
kind: Service
metadata:
  name: payment-orchestrator
  labels:
    tier: backend
spec:
  type:  ClusterIP
  selector:
    app: payment-orchestrator
  ports:
    - port: 80
      targetPort: 8088